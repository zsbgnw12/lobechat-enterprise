# Spec (distilled)

Prototype local-Docker Enterprise Gateway that brokers all enterprise tool calls between a future LobeChat-native UX and legacy systems (gongdan, xiaoshou, cloudcost, kb, ai_search, sandbox, doc.generate). See `.omc/plans/autopilot-impl.md` for full contract. Acceptance = `docker compose up` runs cleanly and the 7 acceptance tests in §Acceptance pass.
